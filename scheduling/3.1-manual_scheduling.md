### By default nodeName is not set on yaml file.

You can manually set scheduler by using **Binding**.


To check the scheduler namespaces: `kubectl get pods -n kube-system`

To check the nodes: `kubectl get nodes`